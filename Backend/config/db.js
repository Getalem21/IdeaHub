/*import mysql from "mysql2/promise";

const db = await mysql.createConnection({
  host: "localhost", // change if needed
  user: "root",
  password: "",
  database: "IdeaHub", // change if needed
});

export default db;
*/

